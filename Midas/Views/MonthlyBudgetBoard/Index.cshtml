@model IEnumerable<Midas_Models.MonthlyBudgetBoard.MonthlyBudgetBoardListItem>

<h1>Monthly Board Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table boardTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MonthlyBudgetBoardId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MonthlyBudgetBoardName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BudgetBoardName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MonthlyBudgetBoardId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MonthlyBudgetBoardName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BudgetBoardName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new {  id=item.MonthlyBudgetBoardId }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
